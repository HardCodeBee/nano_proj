ROC-native synthetic dataset target directory.

Use `scripts/generate_rocstories_synthetic.py` with ROCStories as the source corpus and then
prepare the accepted JSONL into this directory so training configs can refer to the dataset as
`rocstories_synth_roc_native`.
